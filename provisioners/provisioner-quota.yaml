apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: provisioner-quota
  namespace: provisioners
spec:
  replicas: 1
  template:
    metadata:
      labels:
        run: provquota
    spec:
      containers:
      - name: git-sync
        image: lukaszbielinski/git-sync:0.7
        imagePullPolicy: Always
        resources:
          requests:
            memory: "10Mi"
            cpu: "10m"
          limits:
            memory: "50Mi"
            cpu: "20m"
        volumeMounts:
        - name: markdown
          mountPath: /git
        - name: klucze
          mountPath: /root/.ssh/klucze
        env:
        - name: GIT_SYNC_REPO
          value: https://github.com/lukasz-bielinski/test-quota
        - name: GIT_SYNC_WAIT
          value: "12"
        - name: GIT_SYNC_DEST
          value: /git


      - name: prov
        image: lukaszbielinski/kubectl-image:0.12-quota
        imagePullPolicy: Always
        resources:
          requests:
            memory: "50Mi"
            cpu: "50m"
          limits:
            memory: "150Mi"
            cpu: "100m"
        volumeMounts:
        - name: markdown
          mountPath: /src
        env:
        - name: NAMESPACE
          value:


      volumes:
      - name: markdown
        emptyDir: {}
      - name: klucze
        secret:
          secretName: klucze
